export default
(participantArray,
goalTitle,
description,
startDate,
dueDate,
priorityId,
goalTypeId
) => ({
  participantArray,
  goalTitle,
  description,
  startDate,
  dueDate,
  priorityId,
  goalTypeId
})
