export default (
  type,
  dependendenId,
  procedureId,
  diagnosisText,
  orNumber,
  orDate,
  attachments
) => ({
  type,
  dependendenId,
  procedureId,
  diagnosisText,
  orNumber,
  orDate,
  attachments
})
