export default (
  applicationMode,
  dependendenId,
  procedureId,
  orNumber,
  orDate,
  attachments
) => ({
  applicationMode,
  dependendenId,
  procedureId,
  orNumber,
  orDate,
  attachments
})
