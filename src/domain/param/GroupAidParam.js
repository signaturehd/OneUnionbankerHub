export default (dependentId, desiredAmount, effectiveDate, company, durationOfPaymentId, file1, file2) => ({
    dependentId,
    desiredAmount,
    effectiveDate,
    company,
    durationOfPaymentId,
    file1,
    file2
  })
