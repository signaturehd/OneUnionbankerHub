export default (dependentId, desiredAmount, effectiveDate, company, durationOfPaymentId, attachments) => ({
    dependentId,
    desiredAmount,
    effectiveDate,
    company,
    durationOfPaymentId,
    attachments
  })
