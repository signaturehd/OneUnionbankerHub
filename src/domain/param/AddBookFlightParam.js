export default (
  requestId,
  totalCostOfFlight,
  totalServiceCharge,
  departureTime,
  returnTime,
  isDomestic,
  attachmentsData) => ({
    requestId,
    totalCostOfFlight,
    totalServiceCharge,
    departureTime,
    returnTime,
    isDomestic,
    attachmentsData
  })
