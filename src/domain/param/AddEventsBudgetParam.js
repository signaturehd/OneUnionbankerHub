export default (
  requestId,
  venueName,
  address,
  region,
  province,
  city,
  date,
  attendees,
) => ({
  requestId,
  venueName,
  address,
  region,
  province,
  city,
  date,
  attendees,
})
