export default (
  typeDeliveryId,
  deliveryDate,
  amount,
  preferredDate,
  orNumberText,
  attachmentArray
  ) => ({
    typeDeliveryId,
    deliveryDate,
    amount,
    preferredDate,
    orNumberText,
    attachmentArray
  })
