export default (
  brand,
  model,
  screenSize,
  color,
  estimatedAmount,
  terms,
  deliveryOption,
  qoutation,
) => ({
  brand,
  model,
  screenSize,
  color,
  estimatedAmount,
  terms,
  deliveryOption,
  attachments: qoutation,
})
