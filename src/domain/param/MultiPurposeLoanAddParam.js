export default (
  dealerName,
  amountValue,
  modeOfLoanId,
  loanType,
  poaText,
  termId,
  selectedOffsetLoan,
  attachments
  ) => ({
    dealerName,
    amountValue,
    modeOfLoanId,
    loanType,
    poaText,
    termId,
    selectedOffsetLoan,
    attachments
  })
